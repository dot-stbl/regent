# Style shaping — captured house-rules from plexor dogfood

> Per [ADR-0001](https://github.com/dot-stbl/regent/blob/main/docs/adr/0001-regent-positioning-bridge-not-linter.md): prefer native tool. Regent ast rule only when native can't.
> Source: [#110 plexor dogfood](https://github.com/dot-stbl/regent/issues/110) (Phase 1.5). Captured verdicts in chronological order.

## Confirmed patterns (user-approved, OK as-is)

| # | Pattern | Source file:line | Status | Where it goes |
|---|---|---|---|---|
| 1 | `file static class <Name>RouteNames` with const route ids | `plexor/src/modules/Plexor.Modules.Clusters/.../Controllers/VmsController.cs:44-66` | native-covers? **no** — convention about organization, no analyzer catches it | planned regent ast rule → `tools/audit/rules/csharp.api.route-name-const.lint.ts` |
| 2 | Primary ctor for DI (9 params) | `VmsController.cs:96-107` | native-covers? **partial** — `IDE0290:warning` in plexor; regent can enforce harder | documentation; possibly regent rule to upgrade severity |
| 3 | Single-endpoint deps via `[FromServices]` (NOT in ctor) | `VmsController.cs:96-107` (analysis: 8/9 deps are single-endpoint) | native-covers? **no** — no MA/RCS/CA/IDE covers this | planned regent ast rule → `tools/audit/rules/csharp.di.ctor-with-single-use.lint.ts` |
| 4 | XML doc on public surface (controllers, interfaces, public members) | universal (e.g. `VmsController.cs:111-130`) | native-covers? **YES** — `CS1591 severity=error` already enforces | nothing to add |
| 5 | **All banned abbreviations in parameter names** — `ex`/`e` (exception), **`ct` (cancellationToken)** — should be fully spelled. `ct` IS already in `naming-and-types.md` §1 banned list (with self-audit grep), but project code still uses it. Real offenders: `WorkloadCommandHandlers.cs:148` `CancellationToken ct = default`. Roslyn doesn't catch parameter-name abbreviations; only regent ast rule would. User: "сокращение в виде конслейшн токен, а называется параметр CT. Это хуйня, надо полноценно назвать. Это надо какой-то warning, либо предупреждение делать на уровне regent" | `plexor/.../WorkloadCommandHandlers.cs:148` (`CancellationToken ct = default`) — also seen in `VmsController.cs`, `SigningKeyBootstrapper.cs`, `ICommandHandler<T, R>.HandleAsync(..., CancellationToken ct = default)` | native-covers? **partial** — `naming-and-types.md` §1 lists `ct`/`req`/`resp`/`err`/`msg`/`svc`/`u`/`x`/`tmp` as banned, but no analyzer enforces. Self-audit grep exists in the rule doc but isn't part of any build gate | house-rule → planned regent ast rule → `tools/audit/rules/csharp.naming.no-banned-abbrevs-in-params.lint.ts`. Detects any of the banned abbreviation names (`ct`, `ex`, `req`, `resp`, `err`, `msg`, `svc`, `u`, `x`, `tmp`, + `e`, `ctx`, `cfg`, `mgr`, `dto`, `dict`, `str`, `arr`, `val`, `num`, `cb`, `fn`, `impl`, `doc`, `src`, `dst`, `buf`, `n`) as parameter names → suggest the full word (`cancellationToken`, `exception`, etc.) |

## Open questions (need user verdict)

| # | Pattern | Source | Question | Status |
|---|---|---|---|---|
| 6 | ~~4 command records with same signature `(WorkloadId, bool, Guid, Guid)`~~ | `plexor/src/modules/.../Application/Vms/VmCommands.cs` | **CLOSED — by design.** `ICommandHandler<TCommand, TResponse>` requires distinct types; signature duplication is the natural consequence. | RESOLVED |

## Confirmed patterns (user-approved, OK as-is) — continued

| # | Pattern | Source | Status | Where it goes |
|---|---|---|---|---|
| 7 | FluentValidator: `AbstractValidator<T>` + `static` lambdas + `RuleFor().MaximumLength(const).When(...)` for optional fields | `plexor/.../Validation/CreateVmRequestValidator.cs:26-82` | native-covers? **YES** — `anti-patterns.md` §4 + `csharp.api.modelstate-adderror` regent rule already enforce "use FluentValidator, not ModelState" | OK as-is; **no new rule needed**. (User feedback: don't drill into specific check values like 64 chars, 64KiB — those are domain choices, not style) |
| 8 | `CancellationToken cancellationToken = default` — last param, with default. **AND** every nested `await` inside such a method must thread it. User: "всегда пишу дефолт, это база", "missing-CT-thread rule нужна" | universal — `SigningKeyBootstrapper.cs:55-103`, `VmsController.cs:56-58,86-88,112-114,137-139` | native-covers? **partial** — `CA1068:error` (CT last), `MA0040:warning` (forward CT). **Missing**: a rule that detects "method takes CT, but some nested `await` doesn't pass it" — currently silent bug, caught only in review | house-rule → planned regent ast rule → `tools/audit/rules/csharp.async.missing-ct-thread.lint.ts`. Two parts: (a) `= default` last-param — extend `naming-and-types.md` (b) missing-ct-thread — new AST rule that walks every `await` in a method with CT param and flags those that don't pass `cancellationToken` |
| 9 | ASP.NET Core `IExceptionHandler` + `ProblemDetails` (BCL) with custom handlers per module. Domain separation: each module has its own `FooExceptionHandler : IExceptionHandler` returning `bool` (TryHandle pattern). `Type` URI stable, `Title` = error code, `Detail` = safe message, `Status` per `MapStatus()` | `plexor/.../Errors/IdentityExceptionHandler.cs:51-67` + `ClustersExceptionHandler.cs` (sibling) | native-covers? **YES** — `error-mapping.md` + `problem-details.md` (global) + `csharp.api.adhoc-error-envelope` + `csharp.api.modelstate-adderror` (regent bundle) | OK as-is. (User feedback: framework-specific, the "one canonical exception handler per module" pattern is by design when the project has domain separation. Don't enforce "one global handler" — that would be wrong for multi-module projects) |
| 10 | **`Result<T>` pattern REMOVED.** Domain factories throw `IdentityException` for invariant violations; `Nullable<T>` handles "expected miss" cases. `Result<T>` is overkill for C# — verbose, no value, clashes with `throw` discipline. User: "Result pattern — хуйня, неприменимая к шарпам. В шарпах nullable есть и вполне себе достойно справляется. Нахуй надо. Я бы даже правила это выпилил" | `plexor/.../Domain/ValueObjects/Email.cs:44-61` + 43 `throw new IdentityException(...)` callsites across 9 files in `Plexor.Modules.Sigil.*` | **action required** — current global rule `error-mapping.md` §3 explicitly recommends `Result<T>` for expected outcomes. **DROP that recommendation**; keep only "throw for invariant / infrastructure failure". Also update the `Result<T>` references in `exceptions.md` and `anti-patterns.md` if any. Audit: `rg -n "Result<\|Result\.Ok\|Result\.Err" ~/.agents/rules/csharp/ \| rg "Result<" — should return 0 hits after fix |
| 11 | **Tests are NOT regent's concern.** Test files (`test/`, `*Test*.cs`, `*Tests.cs`, `*.UnitTests.cs`, `*.IntegrationTests.cs`) should be **excluded from regent checks** in user projects. Test naming (`<Subject>Should`), `DisplayName` BDD format, `Shouldly` only, `NSubstitute` discipline, AAA structure, etc. — these are global rules but NOT enforced via regent. Test style is project-local, chosen per project. User: "в моих проектах шерповых, нахуй не надо правила для тестов. Тесты — это тесты, хули там проверять. Во всех проектах могут быть разные тестирования, в моменте выбираются окружение тестов" | `plexor/tests/unit/Plexor.Modules.Clusters.Unit/Clusters/CreateClusterCommandHandlerShould.cs:1-80` (sample — does follow rules, but as convention, not enforcement) | **action required** — `testing-unit.md` global rule currently mandates strict conventions. **In regent's own dogfooding** (regent codebase) — rules apply (this is where they're authored). **For external projects** (plexor/console.x/tessera) — rules should be EXCLUDED via `.regentrc.yaml` `excludePaths: ['**/test*/**', '**/*.Test*.cs']`. **No regent rule in the bundle** should target test files. `tools/audit/rules/*.lint.ts` for production code only. |
| 12 | **"Max 3 .cs files per folder"** (from `folder-organization.md` §4) — rule exists, **never enforced**, agent routinely drops 8 files in one folder. User: "агент нас создает 8 файлов в одной папке, вообще хуй забьет что там граница. На нее бы правило, наверное, сделать" | universal (will check console.x) | native-covers? **NO** — no Roslyn/dotnet tool enforces directory-level caps. The rule is in the doc but invisible to the build gate | house-rule → planned regent **structural** rule (not AST/regex). New kind: `defineStructureRule` or similar that walks file system + checks file counts per directory against a configurable cap. Reuses the existing `defineAstRule` shape but operates on the directory tree. Where: `tools/audit/rules/structure.folder-cap.lint.ts` (or similar) |
| 13 | **Don't materialize `IEnumerable<T>` to `List<T>` for "snapshot" semantics when not needed.** If the upstream is stable (DI registration), iterate directly. If snapshot is genuinely needed, use `T[]` (array) — explicit fixed-size, less memory than `List<T>`. User: "ToList() — хуетень. Какой нахуй лист? Почему не array, блять?" | `console.x/.../IngestJobRunner.cs:52` (`var snapshot = descriptors.ToList();`) — `descriptors` is already `IEnumerable<IngestJobDescriptor>`, no need to materialize | native-covers? **partial** — `code-shape.md` §7 says "минимально достаточный тип" but doesn't explicitly say "no needless ToList()". The bigger issue: agent defaults to `.ToList()` when an `IEnumerable` would do | house-rule → planned regent ast rule → `tools/audit/rules/csharp.collections.unnecessary-tolist.lint.ts`. Detects `var x = someIEnumerable.ToList();` immediately followed by `foreach` (the canonical needless materialization) |
| 14 | **Use abstract `ScheduledWorkerBase` (or equivalent) for non-trivial scheduled work.** Raw `BackgroundService` is OK only for minimal cases (≤ 3 services, app-lifetime). User: "желательно делать абстракцию, но если прям что-то минимальное, можно и бэкграунд сервис" | `console.x/.../IngestJobRunner.cs:37-42` — uses raw `BackgroundService`. Could be `ScheduledWorkerBase` with `ExecuteCycleAsync` (per `code-shape.md` §9.3) | native-covers? **NO** — Roslyn can't tell "non-trivial scheduled work" from "minimal" | house-rule → documented convention only (no executable rule). Update `code-shape.md` §9.3 to be more explicit about the ≤3 services threshold. Optionally: regent ast rule that flags `class X : BackgroundService` whose `ExecuteAsync` body is > 30 lines (proxy for non-trivial) — but this is heuristic |
| 15 | **Stubs / fixtures / test-helpers are NOT regent's concern.** `*Stub.cs` (`StubAllianceDspClient`), `*Fixture.cs` (response fixtures), or files under `Stubs/`, `Fixtures/`, `TestHelpers/` directories should be **excluded from regent checks**. User: "stub — это же стаб, хули там правила? Там по три строчки кода. Stub-файлы можно хоть миллиард болей нахуярить, а если именно полноценный рабочий класс, то да, там надо ограничивать 100%" | `console.x/.../Hybrid.Engine.Delivery/StubAllianceDspClient.cs` (618 lines, ~17 `private static <Name>Ok()` methods — fine for stub, would NOT be fine in production code) | **action required** — `.regentrc.yaml` should add `excludePaths: ['**/*Stub.cs', '**/*Fixture.cs', '**/Stubs/**', '**/Fixtures/**', '**/TestHelpers/**']`. Document this clearly. **Also**: regent should support a `#pragma regent disable` directive (analogous to `#pragma warning disable`) for fine-grained file-level exclusion when the path-based exclusion doesn't fit. Currently regent doesn't have this — **feature request** |
| 16 | **No fully-qualified type names in params/returns when `using` would do.** `params System.Reflection.Assembly[]` should be `using System.Reflection;` + `params Assembly[]`. User: "System.Reflection.Assembly — я забыл, как называется, когда ты излишне перечисляешь типы, когда можно using добавить. Вот это меня очень сильно раздражает" | `console.x/.../Engine.Assembly/EngineInstaller.cs:37` (`params System.Reflection.Assembly[] domainEventAssemblies`) | native-covers? **partial** — JetBrains Rider has "use explicit using" suggestion; `dotnet format` doesn't move fully-qualified to using. No Roslyn rule. | house-rule → planned regent ast rule → `tools/audit/rules/csharp.usings.no-fully-qualified-in-signature.lint.ts`. Detects parameter/return/local types that are fully-qualified when the namespace is in the global usings or could be added |
| 17 | **`ArgumentException.ThrowIfNullOrWhiteSpace()` is BANNED — use semantic check instead.** Already in `code-shape.md` §11, but the user re-emphasizes as a real offender in console.x. Reason: it's argument-validation wrapped in framework helper, when the natural check is `if (string.IsNullOrWhiteSpace(x))` (semantic check, not argument validation). User: "Аргумент Эксепшн. Трав. Ту из знал. Надо убирать. Это кринж. Можно просто сделать проверку на white space у стринги — более лаконично и естественнее" | `console.x/.../Engine.Assembly/BannerContextBuilder.cs:32` (`ArgumentException.ThrowIfNullOrWhiteSpace(bannerId);`) | native-covers? **YES** — `code-shape.md` §11 explicitly bans. Plus `naming-and-types.md` §1 (trust the signature) | OK as-is (rule already exists in global rules). **Action**: the rule exists but the project doesn't follow it. Need to make regent catch it. Add to `tools/audit/rules/` if not there, AND ensure the rule is in `console.x/.editorconfig` as an error or has a regent ast rule that flags it. **`rg -n "Argument.*\.ThrowIf" console.x/src/`** should return 0 hits after fix |
| 18 | **2 `private static` methods in production class = violation.** `ExtractHexId` + `ResolveSchemaTable` should be in `file static class AffectedEntityResolverHelpers`. No exception for reflection classes. User: "приват статик методы, которые запрещены, получается, процедурный силь" | `console.x/.../Engine.PreAggregation/AffectedEntityResolver.cs:110-150` (2 `private static` methods) | native-covers? **partial** — `code-shape.md` §1a bans, but no analyzer enforces | OK as-is (rule exists). **Action**: regent ast rule should flag `private static` methods in `internal`/`public` classes that are NOT in `file static class`. Where: `tools/audit/rules/csharp.code-shape.private-static-method.lint.ts` |
| 19 | **Tuples BANNED in user code GLOBALLY** (not just public API). Current rule in `naming-and-types.md` §3 / `anti-patterns.md` §3 limits to public API. User wants full ban: `Dictionary<string, (string Schema, string Table)>` is a violation. Tuple from third-party code (Refit, etc.) is unavoidable. User: "таплы я бы вообще запретил вообще глобально. То есть это прям запретить таплы в пользовательском коде. То есть, если код из стороннего таплы, то там уж, извините, надо будет использовать" | `console.x/.../Engine.PreAggregation/AffectedEntityResolver.cs:54,134` (`Dictionary<string, (string Schema, string Table)>` + `private static (string Schema, string Table)? ResolveSchemaTable(...)`) | native-covers? **YES** — `naming-and-types.md` §3 / `anti-patterns.md` §3 ban tuples in public API. **Need to expand** to all user code (private fields, locals, method returns all banned). Roslyn has no rule for this — needs regent | **action required** — update `naming-and-types.md` §3 + `anti-patterns.md` §3 to ban tuples everywhere in user code (not just public API). Also need regent ast rule: `csharp.naming.no-tuples-in-user-code.lint.ts` that flags `(Type1, Type2)` declarations (fields, locals, returns) anywhere in `*.cs` files |
| 20 | **Braces ALWAYS — no bare `if (...) continue;`.** Already in `code-shape.md` §5 (severity=error). Real offenders exist. User: "Где фигурные скобки блять? Почему у нас используются без фигурных скобок? Python-style one-liners, я вообще не приму, фигурные скобки должны быть всегда. Это отвратительно выглядит и только ухудшает чтение" | `console.x/.../Engine.PreAggregation/AffectedEntityResolver.cs:76-95` (4 consecutive `if (...) continue;` without braces) | native-covers? **YES** — `code-shape.md` §5 mandates. `csharp_prefer_braces = true:error` (IDE0011) | OK as-is (rule exists). **Action**: regent project should fail-build on these. Verify console.x `.editorconfig` has `dotnet_diagnostic.IDE0011.severity = error`. If yes — the file has `#pragma` suppression or rule isn't being applied. Audit `rg -n "pragma.*disable.*IDE0011\|IDE0011" console.x/src/` |
| 21 | **Magic strings in static arrays/dictionaries → named constants.** `"Id"`, `"BannerId"`, `"CampaignId"` should be in `file static class PropertyNames { public const string Id = "Id"; public const string BannerId = "BannerId"; ... }`. User: "Константные поля без наймоффа, это ужас. Магические строки бесконечные, просто кошмар" | `console.x/.../Engine.PreAggregation/AffectedEntityResolver.cs:38-47` (PriorityProperties array) + `PropertyToTable` dictionary keys | native-covers? **NO** — no Roslyn/dotnet rule enforces "named constants for repeated strings". `constructors-and-fields.md` has the `file static class <Name>Constants` pattern as recommendation but no enforcement | house-rule → planned regent ast rule → `tools/audit/rules/csharp.usings.named-constants-for-repeated-strings.lint.ts`. Detects: string literal used in 2+ collection-key positions across the file → suggest extracting to `file static class <Name>Constants` |
| 22 | **Trivial `ToString()` that just returns a single field is redundant.** For string-typed value objects, callers can use `$"{Value}"` or `"" + Value` directly — the compiler emits the same `String.Concat` / interpolation handler call. Don't write `public override string ToString() { return Value; }` as a 3-line method when the inline expression at the callsite is shorter and equally readable. User: "ToString можно заменять через `$"{Value}"` или `"" + Value` — компилируется в то же самое. Меньше строк занимает. Тустринг то есть ты просто оборачиваешь значение и это компилируется в то же самое" | `plexor/.../Domain/ValueObjects/PermissionScope.cs:55` (`public override string ToString() { return Value; }`) | native-covers? **NO** — Roslyn doesn't flag "trivial ToString". IDE has hints in some cases | house-rule → planned regent ast rule → `tools/audit/rules/csharp.code-shape.no-trivial-tostring.lint.ts`. Detects: `public override string ToString() { return <single-field>; }` or expression-bodied `{ return <field>; }` — fire "use `$"{field}"` at callsite instead". **Note**: this is a soft rule, NOT a hard ban — for value objects with non-trivial format (`$"id={Id}, name={Name}"`), the method IS the right call. The rule only fires on single-field returns |
| 23 | **Refit interface: DTO records MUST go in a separate file (`Models/` subfolder or sibling).** Interface file = methods + attributes only. Inline records break `anti-patterns.md` §2 ("DTO в отдельный файл"). User: "В рефит клиентах я бы выносил бы в отдельный файл модельки. Это было бы гораздо лучше. То есть было бы даже два файла, типа контакты и клиент" | `console.x/.../IDspClient.cs:518-554` — 7 DTO records inline at the bottom of the Refit interface file | native-covers? **NO** — no rule enforces "Refit interface = no DTO types in same file". Roslyn doesn't distinguish Refit files from other code | house-rule → planned regent ast rule → `tools/audit/rules/csharp.http.refit-no-inline-models.lint.ts`. Detects: `record` or `class` declared in a file that contains a `public interface` with Refit attributes (`[Get]`, `[Post]`, etc.). Fire "extract DTOs to a separate file" |
| 24 | **`Task<IApiResponse<T>>` return is REQUIRED for Refit clients** — preserves HTTP status code, response headers, error envelope. User: "Это обязательно надо в рефите использовать" | `console.x/.../IDspClient.cs:60-505` — all 50+ methods use `Task<IApiResponse<T>>` | native-covers? **YES** — already in `http-resilience-refit.md` as canonical pattern | OK as-is (rule exists). No action |
| 25 | **"File > 300 lines" rule DOES NOT apply to Refit interfaces.** Class layout §1b triggers a refactor signal at 300 lines. Refit interfaces are API surface — naturally large (50+ endpoints × 2-3 lines each = 150+ lines just for methods). User: "можно это игнорировать. То есть тут этот класс layout про бизнес-класс какие-то. Вот на них это только работает" | `console.x/.../IDspClient.cs:1-516` (516 lines, all interface) | native-covers? **NO** — current rule says all files ≥ 300 lines are refactor signals. Doesn't distinguish Refit files | **action required** — update `class-layout-and-tooling.md` §1b to exempt files where the only public types are interfaces with Refit attributes. Or add a regent ast rule that exempts such files from the 300-line cap |
| 26 | **Expression-bodied methods are BANNED. Methods = block body `{ }`.** Already in `code-shape.md` §5 (severity=error via `dotnet_diagnostic.IDE0022.severity = error`). Real offenders exist. **ALSO: inline `return X;` без braces** — same rule (Braces REQUIRED always, §5). User: "Я вижу инлайновые returные, которые недопустимы. Я вижу экспресси, которого не должно быть" | `console.x/.../Hybrid.Reporting.CodeGen/EnumLabelCache.cs:30-32,53-56` — `=> EnumLabelCache<T>.Labels;` (expression-bodied) + `if (...) return d;` (inline return без braces) + `private static IReadOnlyDictionary<int, string>? Load() { ... }` (private static method, §1a violation) | native-covers? **YES** — `code-shape.md` §5 + `IDE0011:error` + `IDE0022:error`. Real offenders exist despite the rule | **action required** — verify `console.x/.editorconfig` has `dotnet_diagnostic.IDE0011.severity = error` and `dotnet_diagnostic.IDE0022.severity = error`. If yes — the file has `#pragma` suppression or rule isn't being applied. Audit `rg -n "pragma.*disable.*IDE0011\|IDE0011\|IDE0022" console.x/src/` |
| 27 | **Two related `internal static class` in one file is OK** (structural optimization). User: "У нас две связанные сущности, выполняют не слишком разную работу. Internal — допустимо, оптимизация структуры" | `console.x/.../EnumLabelCache.cs:19-56` (EnumLabelCache<T> + EnumLabelReader) | native-covers? **N/A** — not a violation, just an explicit clarification | OK as-is. **Documentation note**: `folder-organization.md` §1 rule "1 type per file" allows this exception — small related helper classes (`internal`) can co-locate. Document explicitly so future agents don't over-apply the rule |
| 28 | **`sealed record` for command/query DTOs** — canonical CQRS pattern. Positional record with primary-ctor params, all required, defaults where applicable. XML doc on each member (CS1591). User verdict (implicit): cluster commands all follow this pattern | `plexor/.../Application/Clusters/ClusterCommands.cs:33-80` — `CreateClusterCommand`, `UpdateClusterCommand`, `DeleteClusterCommand`, `RotateJoinTokenCommand`, `GetClusterQuery`, `ListClustersQuery` | native-covers? **YES** — `naming-and-types.md` §3 (record for value-equality, with-expressions, immutable). `CS1591` for docs. | OK as-is. Nothing to add |
| 29 | **`sealed class` with `init`-only properties for projections / DTOs** that need EF Core LINQ projection (object-initializer syntax `Select(x => new T { Prop = x.Prop })`). Mapperly source-gen friendly. **Not record** — record's positional-ctor breaks EF's `new X { ... }` projection syntax. User verdict (implicit): `ClusterSummary` and `ClusterDetail` follow this | `plexor/.../Application/Clusters/ClusterCommands.cs:90-180` (ClusterSummary + ClusterDetail) | native-covers? **YES** — `naming-and-types.md` §3 (sealed by default), `mapping.md` (Mapperly) | OK as-is. Nothing to add |
| 30 | **Collection types in user code: prefer `IReadOnlyCollection<T>` over `IReadOnlyList<T>`.** `List<T>` is the worst default (most agents reach for it). `T[]` (array) when size is known. `IReadOnlyList<T>` ONLY for mutation-then-read (1% of cases — internal collections being mutated then exposed). `List<T>` should be limited to ~1% of cases (local mutation only). User: "Лист это хуедень собачья. Это должно быть ограничено всегда. Лист нужен в одном проценте. Использовать надо IReadOnlyCollection, массив сам по себе, и специализированные интерфейсы. ICollection, например, для EF Core уже материализованы, но не совсем. В основном стараться IReadOnly-коллекции. Если нужна изменяемая коллекция, надо использовать тогда уж так уж и быть — IReadOnlyList. Но это 1%" + **anti-pattern `var list = new List<X>(); list.Add(...); list.Add(...); list.Add(...); list.Add(...); return list;`** — even when size is "known", the visual noise is unacceptable. Prefer: `return items.Select(x => ToX(x)).ToArray();` (or just `return items.Select(...)` for lazy). User: "новый лист + 4-Add — это полная залупа. Да, это возможно быстро, но один хуй это медленно. Это выглядит отвратительно. Нам пришел массив параметрах — мы ничего с ним не можем сделать. Если бы пришел IEnumerable или IReadOnlyCollection — мы делаем return и сразу берем Select, и возвращаем, а потом в конце ToArray. Без new-лист. Один хуй у нас там известен размер, но эта вся конструкция гораздо визуальнее понятнее. И потери по производительности — я не думаю, что сильные" | `plexor/.../Application/Clusters/ClusterCommands.cs:164,173` (`IReadOnlyList<string> InstallProviders` + `IReadOnlyList<NodeSummary> Nodes`) — current code uses `IReadOnlyList`, should be `IReadOnlyCollection` per user verdict | native-covers? **partial** — `code-shape.md` §7 mentions minimal-sufficient type, but `IReadOnlyCollection` vs `IReadOnlyList` distinction isn't explicit. `CA1002:warning` (don't expose generic lists) covers `List<T>` only. The `new List + Add × N` pattern is invisible to analyzers. | **action required** — update `code-shape.md` §7 to clarify preference order: `IReadOnlyCollection<T>` (default) > `T[]` (known size) > `IReadOnlyList<T>` (rare, mutation-then-read) > `List<T>` (1%, local mutation). Also fix existing plexor code: change `IReadOnlyList<>` to `IReadOnlyCollection<>` in projections. **Regent ast rule candidate**: `csharp.collections.prefer-ireadonlycollection.lint.ts` — flag `IReadOnlyList<T>` in public API surface, suggest `IReadOnlyCollection<T>`. **Plus**: `csharp.collections.no-new-list-add-allocation.lint.ts` — detect `var x = new List<T>();` followed by `.Add()` calls then `return x;` — suggest `return items.Select(...).ToArray();` instead |
| 31 | **`TimeProvider` injected, never `DateTime.UtcNow`** — for testable "now" semantics. Synthetic example: `public sealed class ExpiringTokenHandler(TimeProvider clock, ITokenRepository repo)` — uses `clock.GetUtcNow()`. | synthetic | native-covers? **partial** — `MA0099:warning` (use Explicit enum value) doesn't catch this. No Roslyn rule for `DateTime.UtcNow`. | house-rule → planned regent ast rule → `tools/audit/rules/csharp.di.no-datetime-utcnow.lint.ts`. Detects `DateTime.UtcNow` / `DateTimeOffset.UtcNow` calls in production code (allow in test code per #11). Suggest `clock.GetUtcNow()` from injected `TimeProvider` |
| 32 | **`nameof(...)` in exceptions and log messages**, not hardcoded strings. Synthetic: `throw new InvalidOperationException($"{nameof(Cluster)} not found");` vs `throw new InvalidOperationException("Cluster not found");`. Refactor-safe | synthetic | native-covers? **NO** — `dotnet_diagnostic.CA1720:warning` flags identifiers containing type name but not the inverse | house-rule → planned regent ast rule → `tools/audit/rules/csharp.usings.prefer-nameof-in-messages.lint.ts`. Soft rule: when an exception/log message contains a substring matching a type/parameter name, suggest `nameof(...)` for refactor-safety |
| 33 | **`is { } x` pattern matching instead of `x is not null` or `x != null`** for nullable checks. Synthetic: `if (await repo.GetAsync(id) is { } entity) return entity;` — `code-shape.md` §1 inline assignment + check | synthetic (matches `Email.cs` style) | native-covers? **YES** — `code-shape.md` §1 + IDE0078:warning (`use pattern matching`) | OK as-is. Rule exists in global rules |
| 34 | **`switch` expression ONLY for value-yielding (return-style) arms.** Single value per arm (`State.A => Step1`). For arms with **multiple statements / throw / complex logic** — use `switch` statement with `case` blocks. Distinction: switch expression = "compute value"; switch statement = "do sequence of actions". User: "returnoвый свеч записывается через `=>`. Если будет switch statement с большими кейсами, где много кода, то да — это statement switch". | synthetic (universal); see also `WorkloadCommandHandlers.cs:165-171` — `commandType = command.Action switch { WorkloadAction.Start => "workload.start", ... _ => throw new ArgumentOutOfRangeException(...) }` — return-value + throw, fits expression form | native-covers? **YES** — `code-shape.md` §1 + `csharp_style_prefer_switch_expression = true:warning` | OK as-is. Rule exists. **Note for clarity**: switch expression is for value-returning, switch statement is for action-doing |
| 35 | **No public fields — properties for everything.** Even for primitives (int, Guid, bool): properties are NOT slower than fields with `get; init;` or `get; set;` — modern JIT inlines them. **Exception**: `public const` and `public static readonly` fields ARE allowed (different category — they can't be properties). User: "если ты делаешь public field — это сокращение, должен с маленькой буквы начинаться (default .NET), но поля просто не надо использовать в открытом виде. В открытом виде воспринимаю только const или static readonly. Static field — это хуйня, лучше делать property с get init. Property не медленнее чем fields. Не надо так делать, использовать property. Если приватные локально использовать field — и сохранять naming без нижнего подчеркивания" | synthetic (universal); `code-shape.md` §3 + `naming-and-types.md` §1 (no `_field` prefix on private fields) | native-covers? **YES** — `CA1051:visible fields should not be used` (warning) + `code-shape.md` §3 | OK as-is. **Clarifications**: (a) const/static readonly fields are OK (special category); (b) property perf is identical to field for primitives — JIT inlines; (c) private fields in classes — no `_` prefix, just use plain PascalCase or camelCase per project convention |
| 36 | **`Async` suffix MANDATORY on every method that returns `Task`/`ValueTask`/`Task<T>`/`ValueTask<T>`**. Synthetic: `public Task<X> GetAsync(...)`, `public async ValueTask<X> CreateAsync(...)`. No exceptions — even trivial async wrappers | synthetic (universal) | native-covers? **YES** — `naming-and-types.md` §1 (Async suffix mandatory) + `VSTHRD200:error` | OK as-is. Rule exists |
| 37 | **`ConfigureAwait(false)` BANNED completely** in user's projects (app code AND library code). Reason: modern .NET runtime handles SynchronizationContext correctly always — `ConfigureAwait(false)` is **meaningless noise** that only hurts readability. It's "old code" relic from .NET 4.x where it was mandatory. Modern `await` always resumes correctly regardless of context. User: "ConfigureAwait — старый код, в старом коде это было обязательно. Сейчас без него нахуй не нужен, потому что dotnet с новым рантаймом — бессмысленная хуйня. Всегда правильно прочитаются, всегда правильный, всегда независимый. Нам нет смысла это управлять, это только ухудшает читаемость кода максимально. ConfigureAwait запрещен просто, блять. Это должно шибки вызывать" | synthetic (universal); should also apply retroactively to existing plexor/console.x code | native-covers? **YES** — `naming-and-types.md` §1 + `CA2007:none` + `MA0004:removed` (2026-07-19) | **action required** — ensure `dotnet_diagnostic.CA2007.severity = none` AND `dotnet_diagnostic.MA0004.severity = error` (force error if it appears) — that way any `ConfigureAwait` becomes a build error. **Plus**: verify `rg -n "ConfigureAwait" plexor/src/ console.x/src/` returns 0 hits — if any hit, fix |
| 38 | **`sealed` on every concrete class** by default. Abstract base / documented extension point are the only exceptions. Synthetic: `public sealed class Foo(...)`, never just `public class Foo(...)` | synthetic (universal) | native-covers? **YES** — `naming-and-types.md` §2 (sealed by default), `CA1852:warning` | OK as-is. Rule exists |
| 39 | **`IReadOnlyCollection<T>` / `IReadOnlyList<T>` in public API surface**, never `List<T>` or `IEnumerable<T>` (unless iteration-only). Synthetic: method returns `Task<IReadOnlyList<X>>` | synthetic (universal) | native-covers? **YES** — `code-shape.md` §7, `CA1002:warning` (don't expose generic lists) | OK as-is. Rule exists |
| 40 | **ALWAYS Refit for HTTP client calls. NEVER bare `HttpClient`, NEVER `IHttpClientFactory` typed-`HttpClient`.** Reason: `HttpClient` class is "low-level ground service" — always needs typed wrapper. Refit generates typed interface from `[Get]`/`[Post]` attributes, gives compile-time check, returns `IApiResponse<T>` for status. `IHttpClientFactory` adds unnecessary indirection — use Refit directly with `services.AddRefitClient<IFooClient>()`. User: "HttpClient фактори — нахуя, нам HTTP клиент факторе. Это само нарушение. HttpClient как класс — базовая сущность, граунд сервис, низкоуровневый, надо всегда дополнять. Дополнение — рефит. **Всегда рефит. Вообще всегда в моих проектах должен быть рефит. Даже через HTTP клиент-фактор.** Рефит — имба" | synthetic (universal); should apply retroactively to existing plexor/console.x code that uses `IHttpClientFactory.CreateClient(...)` | native-covers? **YES** — `http-resilience-refit.md` §1-§2 + `csharp.di.bare-httpclient.lint.ts` (regent rule already exists). For the "no IHttpClientFactory typed HttpClient" rule — **need to add** `csharp.di.no-ihttpclientfactory-typed.lint.ts` | **action required** — (a) verify `csharp.di.bare-httpclient.lint.ts` ships in bundle (b) write new rule `csharp.di.no-ihttpclientfactory-typed.lint.ts` flagging `services.AddHttpClient<IFooClient>(...)` patterns in favor of `AddRefitClient<IFooClient>(...)` (c) update `http-resilience-refit.md` to explicitly say "Refit is the only path; IHttpClientFactory typed clients are deprecated" (d) audit existing code: `rg -n "IHttpClientFactory\|HttpClient " plexor/src/ console.x/src/` |
| 41 | **Async LINQ over EF Core — `.ToListAsync()`, `.FirstOrDefaultAsync()`, never sync `.ToList()` on `IQueryable<T>`.** Synchronous `.ToList()` blocks the thread on DB roundtrip. **Also**: ALL good examples must use `cancellationToken` (full name, never `ct` abbrev — even in synthetic bad examples the counter-example should be perfect). | synthetic (universal) | native-covers? **NO** — Roslyn doesn't know `IQueryable<T>` is async-aware. `MA0040` (forward CT) helps but not the sync-over-async block. | house-rule → planned regent ast rule → `tools/audit/rules/csharp.data.no-sync-over-async.lint.ts`. Detects `.ToList()`, `.FirstOrDefault()`, `.Single()` on `IQueryable<T>` from `DbSet<>` types — flag as "use async variant" |
| 42 | **Use `JsonSerializerOptions.Web` (BCL shared) as the default. Custom `*JsonOptions.Instance` is almost always wrong.** Reason: ASP.NET Core apps configure JSON conventions ONCE in `WebApplicationBuilder` (`builder.Services.ConfigureHttpJsonOptions(...)` sets `JsonSerializerOptions.Web`). All custom naming policies, converters, etc. belong at the app boundary — not per-method `new JsonSerializerOptions()`. If you find yourself needing `PropertyNamingPolicy = JsonNamingPolicy.CamelCase` inside a method, you're parsing wrong somewhere upstream. Special cases → extend `JsonSerializerOptions.Web` via `ConfigureHttpJsonOptions` or `Configure<JsonOptions>`. User: "Почему агенты так хотят себе создать свой *JsonOptions.Instance? У нас есть JsonSerializerOptions.Web — база, которую мы заполняем при web application builder. Должно олицетворять все наше приложение, конвенции, нейминг policy, все. Бывают экстраординарные случаи, но их надо заносить в отдельные классы. А если это уникальный случай — какого хуя он уникальный? Значит у тебя где-то неправильно сделано до этого" | synthetic (universal); applies retroactively — plexor and console.x should use `JsonSerializerOptions.Web` everywhere instead of `*JsonOptions.Instance` | native-covers? **YES** — `anti-patterns.md` §6 (no inline options, prefer `.Web` or `*JsonOptions.Instance`). **Stronger rule than current doc**: drop `*JsonOptions.Instance` recommendation, use ONLY `JsonSerializerOptions.Web` | **action required** — update `anti-patterns.md` §6 to drop `*JsonOptions.Instance` mention; keep only `JsonSerializerOptions.Web` (Tier 1) and `services.ConfigureHttpJsonOptions(...)` for custom app-level config (Tier 2). **Regent ast rule** (if needed): detect `private static readonly JsonSerializerOptions` fields → suggest using `JsonSerializerOptions.Web` instead |
| 42 | **`JsonSerializerOptions` reuse via `*JsonOptions.Instance`** — never `new JsonSerializerOptions()` at callsite. Synthetic: `JsonSerializer.Serialize(payload, MyJsonOptions.Instance)` | synthetic (universal) | native-covers? **YES** — `anti-patterns.md` §6 (no inline options) | OK as-is. Rule exists |
| 43 | **`HealthCheck` pattern via `IHealthCheck`** — not raw endpoints. **Also**: health endpoint path must NOT collide with frontend routes — use a dedicated path (e.g. `/api/health`, `/internal/health`, or behind reverse proxy with a separate `/healthz`). User: "Health чеки должны быть как бы компонуются вместе с апишкой, потому что не всегда ты можешь предоставлять endpoint и просто `/health` — это может быть занято под фронт энд. Нужна определенное разделение фронта-бэка". Synthetic: `services.AddHealthChecks().AddCheck<MyDbHealthCheck>("postgres"); app.MapHealthChecks("/api/health");` (note path — NOT `/health`) | synthetic | native-covers? **NO** — no rule for "must use IHealthCheck" or "health path separate from frontend". Convention only. | house-rule → documented convention only. No executable rule (too architectural for AST detection). **Path discipline**: health endpoint must be on a non-frontend path; convention is `/api/health` (backends) or `/healthz` (k8s liveness/readiness) |
| 44 | **`IAsyncDisposable` over `IDisposable` for async resources** (e.g. `DbContext`, `HttpClient`-wrapping services). Synthetic: `public async ValueTask DisposeAsync() { ... }` instead of (or alongside) `public void Dispose()`. User: "Это пизда, это правильное правило. Можно на уровне JetBrains делается" | synthetic | native-covers? **partial** — `MA0045:none` (don't make sync method async); JetBrains Rider has `AsyncFixer01` flags sync-over-async Dispose but not the inverse. JetBrains has `UseAsyncDisposable` / `PreferAsyncDisposable` heuristic | OK as-is. Convention only + JetBrains Rider analyzers (for users of Rider). No Roslyn/dotnet rule covers the inverse case |
| 45 | **Wire DTO records (HTTP/JSON) must use `[JsonPropertyName]` for non-PascalCase wire fields** OR rename wire field. **ALSO**: never suffix the type name with `Dto` (per `naming-and-types.md` §1 — banned suffix). Synthetic: `public sealed record UserSummary([property: JsonPropertyName("user_id")] Guid UserId)` (NOT `UserDto`). User: "ДТО в названии запрещен. Нельзя использовать ДТО — это кринж. JsonPropertyName, да, неплохо, даже на рекордах" | synthetic | native-covers? **YES** — `json-and-ndjson.md` §2 mandates `[JsonPropertyName]` for external DTOs; `naming-and-types.md` §1 banned `Dto` suffix | OK as-is. Both rules exist. The fix here is to ensure examples follow BOTH rules |
| 46 | **`sealed` discipline on DTO `record` types** — `public sealed record Foo(...)`, not `public record Foo(...)`. Open `record` only when inheritance is intentional | synthetic | native-covers? **YES** — `naming-and-types.md` §2 (sealed by default) | OK as-is. Rule exists |
| 47 | **`sealed partial class` for EF-owned types / Mapperly projections** — partial because source-gen emits the other half. Synthetic: `public sealed partial class UserSummary { ... }` (Mapperly emits `ToSummary`) | synthetic | native-covers? **YES** — `mapping.md` (Mapperly) | OK as-is. Rule exists |
| 48 | **`switch` arm with `_ =>` wildcard preferred over exhaustive `case`** when only a subset of enum cases matter. Agent fills in every enum case automatically, generating noise. **Wildcard preferred**. Real example from plexor: `WorkloadCommandHandlers.cs:165-171` — only 3 `WorkloadAction` cases matter (Start, Stop, Restart), all others throw via `_ => throw new ArgumentOutOfRangeException(nameof(command), command.Action, null)`. The throw + `nameof(command)` provides refactor-safe error. User: `dotnet_diagnostic.IDE0072.severity = none` + `IDE0010.severity = none` in plexor editorconfig — auto-format's exhaustive fill conflicts with wildcard `_ =>` (MA0025 fires from `NotImplementedException`). | real (plexor `WorkloadCommandHandlers.cs:165-171`) | native-covers? **YES** — `dotnet_diagnostic.IDE0072.severity = none` in plexor editorconfig | OK as-is. Rule + suppression in place |
| 49 | **No `// TODO` without an issue reference** — TODO(ANL-1234) form, never bare `// TODO`. Synthetic example | synthetic (per `commit-format.md` discipline) | native-covers? **NO** — Roslyn doesn't enforce TODO format | house-rule → convention only. Document in `CONTRIBUTING.md` |
| 50 | **`switch` expressions with multiple statements per arm** — use `{}` block-arm: `_ => { var x = Compute(); return x; }`. Synthetic | synthetic | native-covers? **YES** — `code-shape.md` §5 (braces always) | OK as-is. Rule exists |
| 51 | **`primary ctor` for ALL DI classes** — even when only 1-2 deps. Synthetic: `public sealed class Foo(IRepo repo, IClock clock)`, never explicit ctor + field. User verdict (implicit) — confirmed in plexor throughout | synthetic (universal) | native-covers? **YES** — `constructors-and-fields.md` (primary ctor preferred) + `IDE0290:warning` in plexor | OK as-is. Rule exists |
| 52 | **`HttpClient` + `IHttpClientFactory` + `AddStandardResilienceHandler()` (Polly)** — never bare HttpClient, never HttpClient without resilience pipeline. Synthetic | synthetic | native-covers? **YES** — `http-resilience-refit.md` §1 | OK as-is. Rule exists |
| 53 | **`Channel<T>` for backpressure in long-running `BackgroundService`** — bounded `Channel.CreateBounded<T>(capacity)` with `BoundedChannelFullMode.Wait`. Synthetic: producer + consumer pattern. `dotnet format` + Roslyn won't catch; convention | synthetic | native-covers? **NO** — Roslyn doesn't know about Channel patterns. | house-rule → documented convention only. No executable rule |
| 54 | **`IDisposable.Dispose(bool disposing)` pattern** — for sealed classes holding unmanaged resources. Synthetic: `public sealed class Foo : IDisposable { private bool disposed; public void Dispose() { Dispose(true); GC.SuppressFinalize(this); } protected virtual void Dispose(bool disposing) { if (!disposed) { ... } disposed = true; } }` — but `class-layout-and-tooling.md` §1a actually says no protected methods either! So sealed class doesn't get the pattern at all. **Reconciled**: sealed class with unmanaged resources should be rare; if you need it, the `file static class <Name>Helpers` for the dispose body. | synthetic | native-covers? **YES** — `CA1063:warning` (implement IDisposable correctly) | OK as-is. Rule exists + §1a reconciliation |
| 55 | **`default` literal over `default(T)`** — `if (x is null) return default;` not `return default(T);`. Synthetic. | synthetic | native-covers? **YES** — IDE0034:warning (use 'default' literal) | OK as-is. Rule exists |
| 56 | **`JsonSerializerOptions` immutability — never mutate a shared instance at runtime**. Synthetic: don't `JsonOptions.Converters.Add(...)` after creation; build a new instance if needed | synthetic (universal) | native-covers? **YES** — `anti-patterns.md` §6 (no inline options, never mutate shared instance) | OK as-is. Rule exists |
| 57 | **No `protected` or `private` methods in production classes** (per `code-shape.md` §1a — only `public`, `internal` allowed). Synthetic: extension via file-static helpers, not via inheritance. The only `protected` exception: explicit design-point (`abstract` base with documented extension). | synthetic (universal) | native-covers? **YES** — `code-shape.md` §1a (Hard rule) | OK as-is. Rule exists |
| 58 | **Inline awaited result when used once.** If you `await` something and immediately use it (pass to next method, return, foreach), **chain it**. Don't assign to a local variable just to use it once. Three sub-cases: (a) `var coll = await repo.GetAllAsync(); foreach (var x in coll) ...` → `foreach (var x in await repo.GetAllAsync()) ...`. (b) `var x = await SomeAsync(); x.Method(); return x;` → `return (await SomeAsync()).Method();` (or chain). (c) `var x = await GetAsync(id); if (x is not null) { x.Use(); }` → `if (await GetAsync(id) is { } x) x.Use();`. User: "Зачем нам в переменную возвращать вызов await, а потом отдельно обработки, когда можно сразу? Вызов await, и у него сразу через цепочку методов. Довольно круто". | synthetic (universal pattern); seen in plexor `BannerContextBuilder.cs:36` (chained into `BuildContext` directly), `SigningKeyBootstrapper.cs:59,94` (chained into `is { }` check). Anti-example: `var keys = await repository.GetActiveAsync(ct); if (keys is { } existing) { return existing; } return null;` → refactor to `if (await repository.GetActiveAsync(ct) is { } existing) return existing; return null;` (1 line shorter, more readable) | native-covers? **NO** — `code-shape.md` §1 has "Inline assignment + check" for null cases but doesn't generalize to "inline await result when used once". `IDE0078:warning` (pattern matching) helps but doesn't catch the wider pattern. | house-rule → planned regent ast rule → `tools/audit/rules/csharp.async.no-redundant-await-variable.lint.ts`. Detects: `var x = await Y();` followed by `x.<single-use>;` (with no other use of `x` in the method body) — suggest inlining the await into the consumer expression |

## Pattern → decision tree (per ADR-0001)

For each captured pattern, run this before deciding where it lives:

```
1. Search native coverage:
   rg -n "dotnet_diagnostic\.<RULE>" .editorconfig
   rg -n "<RULE>" ~/.agents/rules/csharp/
   rg -n "<RULE>" tools/audit/rules/   (existing regent bundle)

2. If native covers:
   → "covered by native" → no regent rule
   → If regent bundle has a duplicate → file bundle-trim issue (Phase 2-A of #110)

3. If native doesn't cover:
   → "house-rule" → write regent ast rule in tools/audit/rules/
   → Add fixture pair (good.cs / bad.cs) per #84 conformance harness
   → Add to test/bundle-conformance.test.ts
```

## Future snippets (planned for upcoming review)

- [ ] `ICommandHandler<TCommand, TResponse>` dispatch pattern (answers #5)
- [ ] FluentValidation validators (`IValidator<CreateVmRequest>` in `VmsController.cs`)
- [ ] Error envelope / `ProblemDetails` mapping (custom exception → status)
- [ ] Logging format (structured `{Action}` placeholders vs interpolation)
- [ ] `CancellationToken` threading in nested async
- [ ] Result/Outcome types vs `throw` for expected outcomes
- [ ] Test pattern (xUnit + NSubstitute + Builders)
- [ ] File organization (1 type per file rule, namespace mirrors path)
- [ ] EF Core owned types vs JSON column (the polymorphic setting pattern)
- [ ] DI lifetimes (Scopped DbContext, Singleton services)
- [ ] `sealed` discipline on controllers/handlers (already covered, but check overrides)

## Process notes

- Working dir for all subsequent reads: `C:\Users\bradw\source\stbl\plexor` (or `console.x` / `tessera` for cross-project comparison)
- Each user verdict appends one row to **Confirmed** (if approved) or **Open** (if needs more discussion)
- After ~10 confirmed patterns, summary + transition to build: regent ast rule PRs in `tools/audit/rules/`
- Cross-reference each pattern with the regent central bundle (`~/.agents/rules/csharp/regent-rules/`) to detect duplicates

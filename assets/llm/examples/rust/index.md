# Rust examples

| Rule | Kind | Description |
|------|------|-------------|
| [rust.unsafe-block](./rust.unsafe-block.md) | detect | require a `// unsafe-allow:` justification on `unsafe { }` blocks |
| [rust.unwrap-in-prod](./rust.unwrap-in-prod.md) | detect | ban `.unwrap()` in production paths; allow in tests |
